-- rustaceanvim — rust-analyzer integration (this IS the Rust LSP setup).
-- "Already lazy": it only activates rust-analyzer on Rust filetypes.
return {
	"mrcjkb/rustaceanvim",
	version = "^6",
	lazy = false,
}
